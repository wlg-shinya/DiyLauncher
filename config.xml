<config>
    <title>DIY Launcher</title>
    <style><![CDATA[
        /* 全体の背景 */
        body { 
            background-color: #222; 
            color: #eee; 
            font-family: "Meiryo", sans-serif;
        }

        /* グリッドレイアウト */
        .grid-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        /* ボタン共通スタイル */
        .launcher-btn {
            background: #444;
            color: white;
            border: 1px solid #555;
            padding: 20px;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.2s;
            text-align: left;
        }
        
        .launcher-btn:hover {
            background: #555;
            transform: scale(1.02);
            border-color: #888;
        }

        /* 特定の強調用クラス */
        .accent {
            color: #4da6ff;
            font-weight: bold;
        }
    ]]></style>
    <layout><![CDATA[
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px;">
            
            <button class="launcher-btn" data-command="notepad.exe">
                📝 <b>メモ帳</b>
            </button>

            <div style="background: #ddd; padding: 10px; border-radius: 8px;">
                <p>ここはグループです</p>
                <span style="cursor:pointer; color:blue;" data-command="calc.exe">
                    🧮 電卓 (Span要素)
                </span>
            </div>

            <button class="launcher-btn" data-command="start https://google.com">
                🌐 Google
            </button>
            
        </div>
    ]]></layout>
</config>