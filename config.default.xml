<config>
    <head>
        <!-- width, height は起動するアプリのウィンドウサイズです -->
        <width>800</width>
        <height>600</height>

        <!-- ここの内容がそのままheadに適用されます -->
        <meta charset="UTF-8" />
        <title>DIY Launcher</title>
        <style>
            body { 
                background-color: #222; 
                color: #fff; 
                font-family: "Meiryo", sans-serif; 
                padding: 20px;
            }
            .grid-container { 
                display: grid; 
                grid-template-columns: 1fr 1fr; 
                gap: 10px; 
                margin-bottom: 20px;
            }
            .launcher-btn { 
                padding: 15px; 
                cursor: pointer; 
                background: #444; 
                color: white; 
                border: 1px solid #666; 
                border-radius: 5px;
                font-size: 14px;
                text-align: left;
                transition: background 0.2s;
            }
            .launcher-btn:hover { background: #555; }

            #console-log {
                width: 100%;
                height: 240px;
                background-color: #1e1e1e;
                color: #00ff00;
                border: 1px solid #444;
                padding: 10px;
                font-family: monospace;
                box-sizing: border-box;
                border-radius: 4px;
                resize: vertical;
            }
        </style>
    </head>
    <body>
        <!-- ここの内容がそのままbodyに適用されます -->
        
        <!-- data-commandで記述されたコマンドをOSで実行します -->
        <div class="grid-container">
            <button class="launcher-btn" data-command="notepad.exe">
                📝 メモ帳
            </button>
            <button class="launcher-btn" data-command="calc.exe">
                🧮 電卓
            </button>
        </div>

        <!-- data-command-log-idで記述されたidを持つ<textarea>/<input>にコマンド出力ログを表示します -->
        <!-- data-command-log-fileで記述されたファイルパスを作成してログを出力します -->
        <div class="grid-container">
            <button class="launcher-btn" 
                data-command="ping google.com -n 3" 
                data-command-log-id="console-log" 
                data-command-log-file="logs/ping_result.log">
                📡 Ping Test (Log & File)
            </button>
        </div>

        <!-- idにdata-command-log-idで記述された文字列を設定するとコマンド出力ログを表示します -->
        <textarea id="console-log" readonly placeholder="ここにコマンドの実行結果が表示されます..."></textarea>
    </body>
</config>